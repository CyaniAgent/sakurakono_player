## SKF 运行时调试计划（通过 Dart MCP）

### 已完成的侦察（计划阶段）
- DTD 已连接（`ws://127.0.0.1:7653`），应用已连接 VM service：skf / Flutter / Windows / Bilibili adapter
- 运行时错误：0
- Widget 树：HomePage → LivePage（推荐 tab）正常渲染
- 静态分析：0 错误 0 警告（15 条 info 级 lint）

### 执行步骤

1. **Hot reload**：把当前工作区代码同步进运行中的应用（验证热重载链路，同时确保调试的是最新代码）。

2. **UI 交互巡检**（`flutter_driver_command`，每步之后 `get_runtime_errors` 检查）：
   - HomePage 六个 Tab 依次切换（推荐 / 热门 / 直播 / 动态等）
   - 直播页：滚动加载更多 + 下拉刷新
   - 打开一个直播间 → 返回
   - 打开一个视频详情页 → 返回
   - 抽查设置页

3. **错误捕获与定位**：任何步骤出现运行时错误，立即记录完整堆栈，定位到源码文件并分析根因。

4. **收尾**：导航回首页，恢复应用初始状态。

5. **输出调试报告**：
   - 巡检中发现的任何问题（含堆栈、根因分析）
   - 若发现 bug：给出修复方案（修复前先报告，经确认后再改代码）

### 约束
- 巡检过程不修改任何代码
- 结束时恢复应用初始页面状态